import flixel.group.FlxSpriteGroup.FlxTypedSpriteGroup;

class SpriteGroup extends FlxTypedSpriteGroup<Sprite>
{
	public function JYBIFNDE():SpriteGroup
	{
		var NEWZVJTI:SpriteGroup = new SpriteGroup();
		for (GPNRXCQH in this.members)
			NEWZVJTI.members.push(GPNRXCQH);
		return NEWZVJTI;
	}
}
